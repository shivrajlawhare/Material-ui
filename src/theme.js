import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
        primary: {
            main: "#FF6969",
            light: "skyblue",
        },
        secondary: {
            main: "#15c630",
        },
        otherColor: {
            mian: "#999"
        },

    }, 
})
